# from matrix import *
# from numpy import *


# Xp  = powers(X,0,n)
# Yp  = powers(Y,1,1)
# Xpt = Xp.transpose()

# a = matmul(linalg.inv(matmul(Xpt,Xp)),matmul(Xpt,Yp))
# a = a[:,0]

# def poly(a,x):
#     x = 0
    
#     for coefficant in range(0,a):
        
        
# linspace(a,b,n).tolist()